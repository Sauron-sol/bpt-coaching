import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-sans",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
});

const playfair = Playfair_Display({
  variable: "--font-heading",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "Baptiste Personal Trainer | Coach Sportif & Nutritionnel Nice - Monaco",
  description:
    "Coach sportif certifié à Nice et Monaco. Coaching privé, programmes à distance, nutrition personnalisée. Transformez votre corps avec un expert depuis 2018.",
  keywords: [
    "coach sportif Nice",
    "personal trainer Monaco",
    "coaching nutrition",
    "coach privé",
    "programme sportif",
  ],
  icons: {
    icon: [
      { url: "/favicon-32x32.png", sizes: "32x32", type: "image/png" },
      { url: "/favicon-16x16.png", sizes: "16x16", type: "image/png" },
    ],
    apple: "/apple-touch-icon.png",
    shortcut: "/favicon.ico",
  },
  openGraph: {
    title: "Baptiste Personal Trainer | Sport & Nutrition",
    description:
      "Coaching sportif et nutritionnel premium à Nice et Monaco. Résultats garantis.",
    type: "website",
    locale: "fr_FR",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="fr" className={`${inter.variable} ${playfair.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
